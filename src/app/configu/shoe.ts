export class shoe{
    constructor(public name: string, public price: number, public color: string, public stock: number){
        this.name = name;
        this.price = price;
        this.color = color;
        this.stock = stock;
    }
    
}

